---
description: {{if eq .ConversationLanguage "ko"}}Context-Isolated 자동 파이프라인 (Run → Sync-Review Loop → Final Merge){{else if eq .ConversationLanguage "ja"}}Context-Isolated自動パイプライン (Run → Sync-Review Loop → Final Merge){{else if eq .ConversationLanguage "zh"}}上下文隔离自动管道 (Run → Sync-Review Loop → Final Merge){{else}}Context-Isolated auto pipeline (Run → Sync-Review Loop → Final Merge){{end}}
argument-hint: "SPEC-XXX [--iterations N] [--skip-run] [--no-copilot]"
---

Use Skill("ae") with arguments: auto $ARGUMENTS
